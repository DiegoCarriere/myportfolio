import React from 'react';
import Navigation from "../components/Navigation";

const Projets = () => {
    return (
        <div className="page">
            <Navigation />
        </div>
    );
};

export default Projets;